<%= form_with(model: opening, local: true) do |form| %>
  <% if opening.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opening.errors.count, "error") %> prohibited this opening from being saved:</h2>

      <ul>
      <% opening.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ecocode %>
    <%= form.select :ecocode, Opening.distinct(:ecocode).order(:ecocode).pluck(:ecocode) %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.select :name, Opening.distinct(:name).order(:name).pluck(:name) %>
  </div>

  <div class="field">
    <%= form.label :variation %>
    <%= form.text_field :variation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
