<p id="notice"><%= notice %></p>

<h1><%= @players.count %> Players</h1>

<table class="table table-striped table-sm table-responsive">
  <thead>
    <tr>
      <th align="center">Name</th>
      <th>Games</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @players.each do |player| %>
      <tr>
        <td><%= player.first_name %>&nbsp;<%= player.last_name %></td>
        <td align="right"><%= link_to "#{player.white_games_count}/#{player.black_games_count}", player_games_path(player) %></td>
        <td><%= link_to 'Show', player, class: 'btn btn-small btn-outline-primary' %></td>
        <td><%= link_to 'Edit', edit_player_path(player), class: 'btn btn-small' %></td>
        <td><%= link_to 'Destroy', player, method: :delete, class: 'btn btn-small btn-outline-danger', data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>